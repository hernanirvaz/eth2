r11 () { local s=hrv-zotac2;local n=movie;local d=ztc1;if [[ $(uname -n) = $s ]];then echo $n $d;rsync --ignore-existing --delete -rz /var/lib/plexmediaserver/mp4/$n $d:;else echo only on $s;fi; }
r12 () { local s=hrv-zotac2;local n=lust ;local d=ztc1;if [[ $(uname -n) = $s ]];then echo $n $d;rsync --ignore-existing --delete -rz /var/lib/plexmediaserver/mp4/$n $d:;else echo only on $s;fi; }
r31 () { local s=hrv-zotac2;local n=tv   ;local d=ztc3;if [[ $(uname -n) = $s ]];then echo $n $d;rsync --ignore-existing --delete -rz /var/lib/plexmediaserver/mp4/$n $d:;else echo only on $s;fi; }

alias nt='sudo iftop -nNPBF $(ip r|sed -En "s%^192.* (192.168.8.[0-9]+) *$%\1%p")'
alias rt='echo meo:$V;telnet 192.168.8.254'
alias xx='exit'
