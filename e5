#!/usr/bin/bash

[[ -d "$1" && -s "$2" ]] || exit 0
n=$(sed -E "s%^(ftv.+)(\.[0-9]{2}\.[0-9]{2}\.[0-9]{2}\.)([a-z]+\.)([a-z]+\.)([a-z]+)(.*)([a-z0-9]{3})\$%\3\4\5\2\7%i" <<< "$(basename "$2")")
[[ -s "$1/$n"         ]] && exit 0
echo "mv \"$2\" \"$1/$n\""
